#include <stdio.h>
#include <stdlib.h>

/**
    arch_estudiantes.dat = T_estudiantes sin ningun orden.
    arch_empleados.dat = T_empleados ordenados por DNI.
1) Cargar el arch_estudiantes.dat a una lista doble, ordenados por DNI.
2) Cargar el arch_empleados.dat a un arbol binario balanceado ordenados por salario.
3) Buscar el empleado con mayor sueldo y si existe un alumno con el mismo DNI sacarlo de la lista.
4)
*/

int main()
{
    printf("Hello world!\n");
    return 0;
}
